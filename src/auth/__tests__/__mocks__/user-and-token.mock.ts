import { mockUser } from '../../../user/__tests__/__mocks__/user.mock';
import { mockTokenString } from '../../../token/__tests__/__mocks__/token.mock';

export const mockUserAndToken = {
  user: mockUser,
  accessToken: mockTokenString,
};
